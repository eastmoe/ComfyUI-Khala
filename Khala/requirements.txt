# Runtime Python dependencies for Khala-Music-Generation.
#
# Notes:
# - This file is intended for the current inference stack, not Megatron training.
# - The project is expected to run on top of an NVIDIA NGC PyTorch image, which
#   already provides CUDA, PyTorch, and several low-level acceleration libraries.
# - Megatron code is vendored in `models/Megatron`, so it is not installed from PyPI.

fastapi
uvicorn
httpx
transformers
omegaconf
lightning
numpy
soundfile
einops
simpy
